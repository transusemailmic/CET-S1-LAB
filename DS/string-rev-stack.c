// Reversing a String using Stack Operation
#include <stdio.h>
#define MAX 100
void main(){
    char s1[MAX];
    char s2[MAX];
    char stack[MAX];
    int top=0;
    printf("Please Enter the String: ");
    scanf("%s",&s1);
    int i=0;
    while(s1[i]!='\0'){
        stack[top]==1
        i++;
    }
}